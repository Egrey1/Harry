# Nathan: Use Classes! (/adminCommands completed)

# Egrey (Idar): !wipe (rename vipe and fix), !add - competed


file edit - vipeCommand, tasklist, __init__(classes), modules(adminCommands), addCallback, config, classes, addModal, /adminCommands